<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.solfood.user.store.MenuMapper">

    <!-- 가게별 메뉴 목록 조회 -->
    <select id="getMenusByStoreId" parameterType="int" resultType="kr.co.solfood.user.store.MenuVO">
        SELECT 
            menu_id as menuId,
            store_id as storeId,
            menu_name as menuName,
            menu_price as menuPrice,
            menu_mainimage as menuMainimage,
            menu_intro as menuIntro
        FROM menu 
        WHERE store_id = #{storeId}
        ORDER BY menu_price ASC
    </select>

    <!-- 메뉴 상세 조회 -->
    <select id="getMenuById" parameterType="int" resultType="kr.co.solfood.user.store.MenuVO">
        SELECT 
            menu_id as menuId,
            store_id as storeId,
            menu_name as menuName,
            menu_price as menuPrice,
            menu_mainimage as menuMainimage,
            menu_intro as menuIntro
        FROM menu 
        WHERE menu_id = #{menuId}
    </select>

</mapper> 