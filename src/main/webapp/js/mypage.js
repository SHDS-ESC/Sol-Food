function logout() {
    showInfoPopup("로그아웃 기능은 구현 예정입니다.");
    // location.href = "/logout";
}

function chargePoint() {
    showInfoPopup("포인트 충전 페이지로 이동!");
    // location.href = "/user/charge";
}
function goInfo() {
    showInfoPopup("내 정보 페이지로 이동!");
    // location.href = "/user/info";
}
function goLike() {
    showInfoPopup("찜 페이지로 이동!");
    // location.href = "/user/like";
}
function goReservation() {
    showInfoPopup("예약 내역 페이지로 이동!");
    // location.href = "/user/reservation";
}
function goPayHistory() {
    showInfoPopup("결제 내역 페이지로 이동!");
    // location.href = "/user/payhistory";
}
